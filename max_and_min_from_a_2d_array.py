import numpy as np
n = np.array([[1,2,3,4],[5,6,7,8]])

np.max(n,axis=0)
np.min(n,axis=1)

'''
if axis = 0 then it means which row has max or min value
if axis = 1 then it means which row and coloum has max or min value


'''
